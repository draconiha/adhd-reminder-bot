name: Refine recurring task UI

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  migrate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Update recurring task UI
        run: |
          python - <<'PY'
          from pathlib import Path
          import json

          path = Path('bot.py')
          text = path.read_text(encoding='utf-8')

          old_list = '''def create_recurring_list_keyboard(tasks, page=0, tasks_per_page=5):
    markup = types.InlineKeyboardMarkup(row_width=1)
    start_idx = page * tasks_per_page
    end_idx = start_idx + tasks_per_page
    current_tasks = tasks[start_idx:end_idx]
    for task in current_tasks:
        (task_id, task_text, recurrence_type, recurrence_days_json,
         reminder_time, remind_before, start_date, end_date, is_active) = task
        short_text = task_text[:30] + "..." if len(task_text) > 30 else task_text
        markup.add(types.InlineKeyboardButton(f"🗑️ {short_text}", callback_data=f"delete_recurring_{task_id}"))
    navigation_buttons = []
    if page > 0:
        navigation_buttons.append(types.InlineKeyboardButton("◀️ Назад", callback_data=f"recurring_page_{page-1}"))
    if end_idx < len(tasks):
        navigation_buttons.append(types.InlineKeyboardButton("Вперед ▶️", callback_data=f"recurring_page_{page+1}"))
    if navigation_buttons:
        markup.row(*navigation_buttons)
    markup.row(
        types.InlineKeyboardButton("🗑️ Удалить все", callback_data="recurring_delete_all_ask"),
        types.InlineKeyboardButton("◀️ Назад к управлению", callback_data="recurring_manage")
    )
    return markup
'''

          new_list = '''def create_recurring_list_keyboard(tasks, page=0, tasks_per_page=5):
    markup = types.InlineKeyboardMarkup(row_width=1)
    start_idx = page * tasks_per_page
    end_idx = start_idx + tasks_per_page
    current_tasks = tasks[start_idx:end_idx]
    for task in current_tasks:
        (task_id, task_text, recurrence_type, recurrence_days_json,
         reminder_time, remind_before, start_date, end_date, is_active) = task
        short_text = task_text[:30] + "..." if len(task_text) > 30 else task_text
        markup.add(types.InlineKeyboardButton(f"🔄 {short_text}", callback_data=f"recurring_view_{task_id}"))
    navigation_buttons = []
    if page > 0:
        navigation_buttons.append(types.InlineKeyboardButton("◀️ Назад", callback_data=f"recurring_page_{page-1}"))
    if end_idx < len(tasks):
        navigation_buttons.append(types.InlineKeyboardButton("Вперед ▶️", callback_data=f"recurring_page_{page+1}"))
    if navigation_buttons:
        markup.row(*navigation_buttons)
    markup.row(
        types.InlineKeyboardButton("🗑️ Удалить все", callback_data="recurring_delete_all_ask"),
        types.InlineKeyboardButton("◀️ Назад к управлению", callback_data="recurring_manage")
    )
    return markup


def create_recurring_details_keyboard(task_id):
    markup = types.InlineKeyboardMarkup(row_width=2)
    markup.add(
        types.InlineKeyboardButton("🗑️ Удалить", callback_data=f"delete_recurring_{task_id}"),
        types.InlineKeyboardButton("◀️ К списку", callback_data="recurring_list")
    )
    return markup


def show_recurring_details(user_id, task_id, message_id=None):
    tasks = get_recurring_tasks(user_id)
    task = next((t for t in tasks if t[0] == task_id), None)

    if not task:
        text = "❌ Повторяющееся дело не найдено."
        if message_id:
            bot.edit_message_text(text, user_id, message_id)
        else:
            bot.send_message(user_id, text)
        return

    (_, task_text, recurrence_type, recurrence_days_json,
     reminder_time, remind_before, start_date, end_date, is_active) = task

    recurrence_names = {
        "daily": "каждый день",
        "weekdays": "по будням",
        "weekends": "по выходным",
        "weekly": "еженедельно",
        "monthly": "ежемесячно",
        "none": "без повтора",
    }
    recurrence_text = recurrence_names.get(recurrence_type, recurrence_type)

    try:
        days = json.loads(recurrence_days_json) if recurrence_days_json else []
    except Exception:
        days = []

    if recurrence_type == "weekly" and days:
        names = ["Пн", "Вт", "Ср", "Чт", "Пт", "Сб", "Вс"]
        recurrence_text += ": " + ", ".join(names[d] for d in days if isinstance(d, int) and 0 <= d <= 6)
    elif recurrence_type == "monthly" and days:
        recurrence_text += ": " + ", ".join(map(str, days))

    start_text = format_date(start_date) if start_date else "не указано"
    end_text = format_date(end_date) if end_date else "без ограничения"

    if reminder_time and reminder_time != 'None':
        reminder_text = reminder_time
        if remind_before:
            if remind_before < 60:
                reminder_text += f" (за {remind_before} мин)"
            elif remind_before == 60:
                reminder_text += " (за 1 час)"
            elif remind_before == 120:
                reminder_text += " (за 2 часа)"
            elif remind_before == 1440:
                reminder_text += " (за день)"
    else:
        reminder_text = "без напоминания"

    status_text = "активно" if is_active else "выключено"
    text = (
        f"🔄 <b>{task_text}</b>\n\n"
        f"📅 Начало: {start_text}\n"
        f"📅 До: {end_text}\n"
        f"🔁 Повтор: {recurrence_text}\n"
        f"⏰ Время: {reminder_text}\n"
        f"📌 Статус: {status_text}"
    )
    markup = create_recurring_details_keyboard(task_id)

    if message_id:
        bot.edit_message_text(text, user_id, message_id, parse_mode='HTML', reply_markup=markup)
    else:
        bot.send_message(user_id, text, parse_mode='HTML', reply_markup=markup)
'''

          if old_list not in text:
              raise SystemExit('Expected recurring list block not found')
          text = text.replace(old_list, new_list, 1)

          old_handler = '''    elif data.startswith('delete_recurring_'):
        task_id = int(data.replace('delete_recurring_', ''))
        delete_recurring_task(task_id, user_id)
        tasks = get_recurring_tasks(user_id)
        if tasks:
            bot.edit_message_text("🔁 Управление повторяющимися делами:", user_id, msg_id,
                                reply_markup=create_recurring_management_keyboard())
        else:
            bot.send_message(user_id, "✅ Повторяющаяся задача удалена!")
        bot.answer_callback_query(call.id)
'''

          new_handler = '''    elif data.startswith('recurring_view_'):
        task_id = int(data.replace('recurring_view_', ''))
        show_recurring_details(user_id, task_id, msg_id)
        bot.answer_callback_query(call.id)

    elif data.startswith('delete_recurring_'):
        task_id = int(data.replace('delete_recurring_', ''))
        delete_recurring_task(task_id, user_id)
        tasks = get_recurring_tasks(user_id)
        if tasks:
            bot.edit_message_text("🔁 Управление повторяющимися делами:", user_id, msg_id,
                                reply_markup=create_recurring_list_keyboard(tasks))
        else:
            bot.edit_message_text("📭 Повторяющихся дел больше нет.", user_id, msg_id,
                                reply_markup=create_recurring_management_keyboard())
        bot.answer_callback_query(call.id, "✅ Удалено")
'''

          if old_handler not in text:
              raise SystemExit('Expected recurring delete handler not found')
          text = text.replace(old_handler, new_handler, 1)

          path.write_text(text, encoding='utf-8')
          print('Recurring UI updated')
          PY

      - name: Check Python syntax
        run: python -m compileall -q .

      - name: Commit changes and remove migration workflow
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add bot.py
          git rm -f .github/workflows/recurring-ui-migration.yml
          git commit -m "Improve recurring task details UI [skip ci]" || exit 0
          git push
